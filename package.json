{
  "name": "subtrack",
  "version": "1.1.0",
  "private": true,
  "description": "SubTrack — installable PWA subscription tracker (vanilla JS, no build step)",
  "scripts": {
    "test": "node scripts/smoke.test.js",
    "icons": "node scripts/gen-icons.js > scripts/icons.json && node scripts/build-manifest.js",
    "shot": "node scripts/shot.js"
  },
  "devDependencies": {
    "jsdom": "*",
    "playwright": "*"
  }
}
